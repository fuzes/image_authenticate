
import 'package:imageauthenticate/image.dart';

class AuthenticateManager {
  List<ImageForAuthenticate> imagesForAuthenticate;

  AuthenticateManager({this.imagesForAuthenticate});

  authenticate(){
  }
}